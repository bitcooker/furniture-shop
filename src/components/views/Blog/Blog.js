import React from 'react';

import styles from './Blog.module.scss';

const Blog = () => (
  <div className={styles.root}>
    <div className='container'>This is Blog</div>
  </div>
);

export default Blog;
